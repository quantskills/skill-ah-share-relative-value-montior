# OpenClaw Runtime Adapter

Before answering an A/H relative-value request, load the repository root `SKILL.md` and use its workflow.

Run the bundled scripts for PandaData and HKMA-backed data access. Company names are valid inputs; stock codes are not a user prerequisite. If approved live data is unavailable, fail closed and state the exact limitation. Do not use web search as a replacement data source or claim that a price gap is executable.
