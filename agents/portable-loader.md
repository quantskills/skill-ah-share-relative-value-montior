# Hermes Runtime Adapter

Load and follow the repository root `SKILL.md` before handling A/H relative-value requests.

Use the bundled Python scripts for data access and computation. Resolve company names through the bundled A/H pair map; do not require users to provide stock codes. Use only PandaData and HKMA through the approved workflow. Do not browse unrelated webpages or substitute stale local snapshots when live data fails. Return reproducible research outputs and clearly state limitations.
